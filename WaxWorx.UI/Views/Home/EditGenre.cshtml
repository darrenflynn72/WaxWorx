@model WaxWorx.UI.ViewModels.GenreViewModel

@{
    ViewData["Title"] = "Edit Genre";
}

<div class="container bg-dark text-light py-4">
    <h2 class="text-uppercase text-info mb-4">Edit Genre</h2>

    <form asp-action="EditGenre" method="post">
        <input type="hidden" name="Id" value="@Model.Id" />

        <div class="form-group">
            <label for="Name">Genre Name</label>
            <input type="text" id="Name" name="Name" class="form-control" value="@Model.Name" required />
        </div>

        <button type="submit" class="btn btn-custom-edit">Save Changes</button>
    </form>
</div>